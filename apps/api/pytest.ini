[pytest]
# Only collect from tests/. scripts/test_*.py are CLI tools, not pytest tests,
# and import the heavy RAG/DB stack that isn't present in the lean CI env.
testpaths = tests
markers =
    eval: marks tests as RAG evaluation tests (may be slow, uses API calls)

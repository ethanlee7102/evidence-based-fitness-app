# direnv: auto-activate the Python venv for the whole apps/api subtree.
# Evaluated with the working dir set to this file's location, so relative paths
# resolve here regardless of which subdirectory you cd into.
export VIRTUAL_ENV="$PWD/venv"
PATH_add "$VIRTUAL_ENV/bin"

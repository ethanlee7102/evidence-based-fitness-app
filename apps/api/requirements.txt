# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9

# Database
supabase>=2.5.0
python-dotenv>=1.0.0

# HTTP client
httpx>=0.27.0

# Auth
python-jose[cryptography]>=3.3.0

# RAG - PDF processing & chunking
docling>=2.0.0
pymupdf>=1.24.0
langchain-text-splitters>=0.2.0

# RAG - cross-encoder reranking (Phase 2). Local ONNX cross-encoder; lazily imported,
# loaded only when RERANK_ENABLED=true. Kept out of requirements-ci.txt for the lean CI.
flashrank>=0.2.10

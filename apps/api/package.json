{
  "name": "@flame-fitness/api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "venv/bin/uvicorn app:app --reload --host 0.0.0.0 --port 8000",
    "start": "venv/bin/uvicorn app:app --host 0.0.0.0 --port 8000",
    "test": "venv/bin/pytest tests/ -v",
    "clean": "rm -rf __pycache__ .pytest_cache"
  }
}
